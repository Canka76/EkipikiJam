using UnityEngine;

public class UltGunGrowing : MonoBehaviour
{
    private GunWithModes _gunWithModes = new GunWithModes();

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (_gunWithModes.currentModeIndex == 3)
        {
            
        }
    }
}
